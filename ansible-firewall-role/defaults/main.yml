---
# defaults file for firewall-role
ufw_list_of_enabled_hosts:
  - 192.168.X.X/24 # Example 

ufw_list_of_opened_ports:
  - '80' # http
  - '443' # https
  - '22' # ssh

firewall_services_redhat:
    - ssh
    - dhcpv6-client

firewall_ports_redhat:
    - '80'
    - '443'
    - '22'